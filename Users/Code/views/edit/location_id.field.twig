{# empty Twig template #}

{% import "form.macro.twig" as forms %}

{% set item = get_context('item') %}
{% set params = { 'country_id': item.country_id } %}

{% set location = callFunction( "Setup.Regions.Code.Models.RegionsModel", 'getRegionsInput', params) %}

{{ forms.input('location_id', 'select', item.location_id, 'Location', null, null, null, null, location)  }}